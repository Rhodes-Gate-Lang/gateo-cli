/**
 * Stable runtime ABI for codegen'd executables (C11).
 *
 * The `gate codegen` command will emit a design translation unit that links
 * with this runtime. Symbol names and structs will be finalized alongside the
 * emitter; this header is a placeholder scaffold.
 */

#ifndef GATE_RT_H
#define GATE_RT_H

/* Placeholder — design_eval(), port tables, and shared types will live here. */

#endif /* GATE_RT_H */
